This project is a responsive student registration form built using HTML, CSS, and JavaScript.
When the user submits the form, the entered data is dynamically added as a new row in the table below the form without refreshing the page.
